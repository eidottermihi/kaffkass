<%= simple_form_for([@user, @holiday]) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :beginning %>
      <%= f.input :till %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>
